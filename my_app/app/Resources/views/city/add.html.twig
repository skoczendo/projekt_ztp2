{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.city.add'|trans }}
{% endblock %}

{% block body %}

    <h1>
        {{ 'title.city.add'|trans }}
    </h1>

    {% include 'default/_form.html.twig' with {
    attr: {
    method: 'post',
    action: url('city_add')
    },
    } %}

    <br>
    <br>
    <div class="btn-group">
        <a href="{{ path('city_index') }}" class="btn btn-default" title="{{ 'action.index'|trans }}">
            {{ 'action.index'|trans }}
        </a>
    </div>
{% endblock %}