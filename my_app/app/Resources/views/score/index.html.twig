{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.scores'|trans }}
{% endblock %}

{% block body %}


    <div class="container">
        {% include 'default/_flash_messages.html.twig' %}
    </div>

    <h1>{{ 'title.scores'|trans }}</h1>
    {% if scores is defined and scores|length %}
        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ 'label.id'|trans }}</th>
                <th>{{ 'label.place'|trans }}</th>
                <th>{{ 'label.points'|trans }}</th>
                <th>{{ 'label.category'|trans }}</th>
                <th>{{ 'label.competition'|trans }}</th>
                <th>{{ 'label.contestant.surname'|trans }}</th>
                <th>{{ 'label.contestant.name'|trans }}</th>
                <th scope="col">
                    <span class="glyphicon glyphicon-cog" aria-hidden="true"></span> {{ 'label.actions'|trans }}
                </th>
            </tr>
            </thead>
            <tbody>
            {% for item in scores %}
                <tr>
                    <td>{{ item.id }}</td>
                    <td>{{ item.place }}</td>
                    <td>{{ item.points }}</td>
                    <td>{{ item.category.name }}</td>
                    <td>{{ item.competition.date|date('Y-m-d') }}</td>
                    <td>{{ item.contestant.surname }}</td>
                    <td>{{ item.contestant.name }}</td>
                    <td>
                        <div class="btn-group" role="group" aria-label="{{ 'label.actions'|trans }}">
                            <a href="{{ path('category_view', {'id': item.id}) }}" class="btn btn-default" title="{{ 'action.view'|trans }}">
                                <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>
                                {{ 'action.view'|trans }}
                            </a>
                        </div>
                    </td>
                    <td>
                        <div class="btn-group">
                            <a href="{{ path('category_edit', {'id': item.id}) }}" class="btn btn-default" title="{{ 'action.edit'|trans }}">
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                {{ 'action.edit'|trans }}
                            </a>
                        </div>
                    </td>
                    <td>
                        <div class="btn-group">
                            <a href="{{ path('category_delete', {'id': item.id}) }}" class="btn btn-default" title="{{ 'action.delete'|trans }}">
                                <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                                {{ 'action.delete'|trans }}
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="btn-group">
            <a href="{{ path('category_add') }}" class="btn btn-default" title="{{ 'action.add'|trans }}">
                {{ 'action.add'|trans }}
            </a>
        </div>

    {% else %}
        <div class="alert alert-warning" role="alert">
            {{ 'message.empty_list'|trans }}
        </div>
    {% endif %}
{#
    <div class="navigation text-center">
        {{ pagerfanta(categories, 'twitter_bootstrap3_translated', { routeName: ('category_index_paginated') }) }}
    </div>
#}
{% endblock %}
