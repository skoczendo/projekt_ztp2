{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.school.edit'|trans }}
{% endblock %}

{% block body %}

    <h1>
        {{ 'title.school.edit'|trans }}
    </h1>

    {% include 'default/_form.html.twig' with {
    attr: {
    method: 'post',
    action: url('school_edit', {'id': school.id})
    },
    } %}

    <br>
    <br>
    <div class="btn-group">
        <a href="{{ path('school_index') }}" class="btn btn-default" title="{{ 'action.index'|trans }}">
            {{ 'action.index'|trans }}
        </a>
    </div>
{% endblock %}